'use client';

import { Container } from '@mantine/core';

// Note: In a real scenario, this would come from a database setting
// For now, let's use the generated image path as a default
export function DashboardLayout() {


  return (
    <Container size="xl" py="xl">
      
    </Container>
  );
}
