'use client';

import PersonalizationLayout from '@/components/layout/personalization/personalization-layout';

/**
 * Personalization settings page for super_admin.
 * Allows customizing space name, logo, colors, etc.
 */
export default function PersonalizationPage() {
  return (
    <PersonalizationLayout />
  );
}
